use 'pip install -r requirements.txt' in your command prompt to install all the required modules directly
after installing the requird modules use 'python app.py' in the command prompt to start the program in the local host
